#####################################################################################
#
# My play-authenticate settings
#
#####################################################################################

play-authenticate {
    
    # Settings for the password-based authentication provider
    # if you are not using it, you can remove this portion of the config file
    password {
        mail {
            verificationLink {
                # Whether the verification link will be HTTPS
                secure=false
            }
            passwordResetLink {
                # Whether the password reset link will be HTTPS
                secure=false
            }
            from {
                # Mailing from address
                email="heroku@unogi.com"

                # Mailing name
                name=heroku.unogi.com
            }
        }
        # Whether to directly log in after the password reset (true)
        # or send the user to the login page (false)
        loginAfterPasswordReset=true
    }

    # Settings for the foursquare-based authentication provider
    # if you are not using it, you can remove this portion of the config file
    foursquare {
        redirectUri {
            # Whether the redirect URI scheme should be HTTP or HTTPS (HTTP by default)
            secure=false

            # You can use this setting to override the automatic detection
            # of the host used for the redirect URI (helpful if your service is running behind a CDN for example)
            #host=yourdomain.com
        }

        # Foursquare credentials
        # Get the credentials here: https://de.foursquare.com/oauth/
        # Remove leading '#' after entering
        clientId=E00FSYHT5EUQFJ12IITUD4ZML53CQJWWZGXVQKPWLQQFBUHG
        clientSecret=I25WNOGEH4BX4UUTIPTXECKIDHUNJBSZ2FHWBSPD20MWFPNP
    }

    # Settings for the facebook-based authentication provider
    # if you are not using it, you can remove this portion of the config file
    facebook {
        redirectUri {
            # Whether the redirect URI scheme should be HTTP or HTTPS (HTTP by default)
            secure=false

            # You can use this setting to override the automatic detection
            # of the host used for the redirect URI (helpful if your service is running behind a CDN for example)
            #host=yourdomain.com
        }

        # Facebook credentials
        # Get them here: https://developers.facebook.com/apps
        # Remove leading '#' after entering
        #
        # knotmatch.com
        #clientId=447167225304606
        #clientSecret=2dbc6e292b26b3d83c1f980686c29377
        clientId=137588573097082
        clientSecret=01cf24af42a470d8a207875f8bd67b0e
        
    }

    # Settings for the google-based authentication provider
    # if you are not using it, you can remove this portion of the config file
    google {
        redirectUri {
            # Whether the redirect URI scheme should be HTTP or HTTPS (HTTP by default)
            secure=false

            # You can use this setting to override the automatic detection
            # of the host used for the redirect URI (helpful if your service is running behind a CDN for example)
            #host=yourdomain.com
        }

        # Google credentials
        # Get them here: https://code.google.com/apis/console
        # Change it here:https://code.google.com/apis/console/#project:421773550365:access
        # Remove leading '#' after entering
        clientId="421773550365-um10r3jik7c6tpplruf3d7mkhrofiepu.apps.googleusercontent.com"
        clientSecret="GdPLj4cUyaFhF1ZBZPqvb6o_"
        # Email address: 421773550365-um10r3jik7c6tpplruf3d7mkhrofiepu@developer.gserviceaccount.com
    }

    # Settings for the OpenID-based authentication provider
    # if you are not using it, you can remove this portion of the config file
    openid {
        redirectUri {
            # Whether the redirect URI scheme should be HTTP or HTTPS (HTTP by default)
            secure=false

            # You can use this setting to override the automatic detection
            # of the host used for the redirect URI (helpful if your service is running behind a CDN for example)
            #host=yourdomain.com
        }
    }
}
